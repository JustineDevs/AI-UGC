import {
  type AppConfig as Configuration,
  loadAppConfig as loadConfiguration,
  validateAppConfig as validateConfiguration,
} from "@ai-ugc/config";

export type { Configuration };
export { loadConfiguration, validateConfiguration };
