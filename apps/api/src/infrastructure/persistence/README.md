# Persistence Adapters

This directory contains the repository adapters for workspace templates,
provider profiles, workflow blueprints, projects, generation sessions, provider
jobs, output assets, and auth sessions.

These adapters persist metadata through the shared database layer rather than
process-local `Map` storage.
