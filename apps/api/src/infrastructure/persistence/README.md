# Persistence Adapters

Persistence adapters for workspace templates, provider profiles, projects,
sessions, and provider jobs will live here as the migration from the older
in-memory runtime continues.
